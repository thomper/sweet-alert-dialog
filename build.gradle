buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.0.0'
    }
}

ext {
    compileSdkVersion = 21
    buildToolsVersion = "19.1.0"
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}
